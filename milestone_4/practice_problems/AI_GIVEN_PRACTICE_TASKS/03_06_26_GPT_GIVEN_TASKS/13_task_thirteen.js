/**
 * Array sort করো (small → big)
 *
 * ```js
 * const numbers = [10, 3, 25, 1, 8];
 * ```
 *
 * Expected: [1,3,8,10,25]
 */

const numbers = [10, 3, 25, 1, 8];
console.log(numbers.sort((a, b) => a - b));
